#include "transport_layer.h"

// Example API for sending data
void send_data(const char *address, const char *data, uint32_t length) {
    // Sends data
}

// Example API for receiving data
void receive_data(char *buffer, uint32_t *length) {
    // Receives data
}

//oh hey, it's working!