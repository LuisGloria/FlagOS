#include "syscalls.h"

int sys_write(const char *buf, uint32_t len) {
    // Placeholder implementation
    return 0; // Return the number of bytes written or an error code
}

int sys_read(char *buf, uint32_t len) {
    // Placeholder implementation
    return 0; // Same thing here
}
